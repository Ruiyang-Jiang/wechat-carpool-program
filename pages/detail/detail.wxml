<!-- 页面骨架：简要 loading -->
<view wx:if="{{isLoading}}" class="page-loading">加载中...</view>

<block wx:else>
  <!-- ======= 行程信息卡 ======= -->
  <view class="ride-card {{detail.isPast?'past':''}}">
    <!-- header -->
    <view class="ride-header">
      <text class="badge {{detail.type==='ride'?'badge-driver':'badge-passenger'}}">
        {{detail.type==='ride'?'找乘客':'找司机'}}
      </text>
      <text class="ride-date {{detail.isPast?'date-past':''}}">
        {{detail.departure_date}} {{detail.departure_time||'00:00'}}
      </text>
      <text wx:if="{{detail.isPast}}" class="expired-badge">已过期</text>
    </view>

    <!-- body -->
    <view class="ride-body">
      <view class="row">
        <text>出发地：</text>
        <text class="bold">{{detail.departure_place.city}}, {{detail.departure_place.state}}</text>
      </view>
      <view class="row">
        <text>目的地：</text>
        <text class="bold">{{detail.arrival_place.city}}, {{detail.arrival_place.state}}</text>
      </view>

      <!-- 途经点显示 -->
      <view wx:if="{{detail.stopovers && detail.stopovers.length}}" class="row">
        <text>途经点：</text>
        <view class="stopovers-display">
          <block wx:for="{{detail.stopovers}}" wx:key="index">
            <text class="stopover-tag">{{item.city}}, {{item.state}}</text>
          </block>
        </view>
      </view>

      <!-- ============ 找乘客（ride）专属字段 ============ -->
      <block wx:if="{{detail.type==='ride'}}">
        <view class="row">
          <text>空余座位：</text><text class="bold">{{detail.empty_seats}}</text>
        </view>

        <!-- 👉 车型：仅当有填写时显示 👈 -->
        <view class="row" wx:if="{{detail.car_model}}">
          <text>车型：</text><text class="bold">{{detail.car_model}}</text>
        </view>
      </block>

      <!-- ============ 找司机（request）专属字段 ============ -->
      <block wx:elif="{{detail.type==='request'}}">
        <view class="row">
          <text>乘坐人数：</text><text class="bold">{{detail.passenger_number}}</text>
        </view>
      </block>

      <!-- 共用字段：价格 -->
      <view class="row">
        <text>价格：$</text><text class="bold">{{detail.price}}</text>
      </view>
      <!-- 经停点价格提示 -->
      <view wx:if="{{detail.stopovers && detail.stopovers.length}}" class="row">
        <text style="font-size:22rpx;color:#999;">到经停点价格需与司机协商</text>
      </view>
    </view>
  </view>

  <!-- ======= 发布者信息 ======= -->
  <view class="publisher-card">
    <view class="publisher-row">
      <text>发布者联系方式：</text>
      <text class="bold">{{userOpenid ? (publisherInfo.wechat || '未提供') : '**********'}}</text>
      <button type="primary" size="mini" bindtap="handleCopyWeChat">{{userOpenid ? '复制' : '登录后复制'}}</button>
    </view>
    <!-- 仅对请求展示参与者联系方式列表 -->
    <view wx:if="{{detail.type==='request' && participantsInfo && participantsInfo.length}}" style="margin-top:12rpx;">
      <block wx:for="{{participantsInfo}}" wx:key="openid">
        <view class="publisher-row">
          <text>{{item.nickName || '参与者乘客微信:'}}</text>
          <text class="bold" style="margin-left:8rpx;">{{userOpenid ? (item.wechat || '未提供') : '**********'}}</text>
          <button wx:if="{{userOpenid && item.wechat}}" type="primary" size="mini" data-wxid="{{item.wechat}}" bindtap="copyParticipantWeChat">复制</button>
        </view>
      </block>
    </view>
  </view>

  <!-- ======= 操作按钮 ======= -->
  <block wx:if="{{userOpenid && userOpenid !== detail.publisher_id}}">
    <button class="action-btn" type="primary" bindtap="takeOrder" wx:if="{{detail.type==='ride'}}">我要乘坐</button>
    <button class="action-btn" type="primary" bindtap="takeOrder" wx:if="{{detail.type==='request' && canDriverAccept}}">我要接单</button>
    <button class="action-btn" type="default"  bindtap="joinAsPassenger" wx:if="{{detail.type==='request' && canJoinAsPassenger}}">加入此请求</button>
  </block>

  <!-- ======= 举报 / 反馈入口 ======= -->
  <view style="padding:20rpx;text-align:center;">
    <button size="mini" bindtap="openFeedback">举报 / 反馈</button>
  </view>
</block>
